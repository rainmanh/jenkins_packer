# .bash_profile
# Get the aliases and functions
if [ -f ~/.bashrc ]; then
. ~/.bashrc
fi

source /etc/default/jenkins

# User specific environment and startup programs
PATH=$PATH

JENKINS_HOME={{ jenkins_home_dir }}

export USER JENKINS_HOME PATH
